---
globs: src/**/*.ts,src/**/*.json
description: 安全性最佳實踐和指南
---

# 安全性指南

## Chrome 擴充功能安全

- 只請求必要的權限（`storage`, `activeTab`）
- 避免使用危險的權限如 `tabs` 或 `<all_urls>`
- 使用 Content Security Policy (CSP) 限制腳本執行

## 資料保護

- 使用 `chrome.storage.local` 而非 `chrome.storage.sync`
- 不儲存敏感用戶資料
- 實現資料加密（如需要）

## 輸入驗證

- 驗證所有用戶輸入
- 使用白名單而非黑名單驗證
- 防止 XSS 攻擊

## 音頻處理安全

- 驗證音頻來源的合法性
- 防止惡意音頻檔案處理
- 限制音頻處理的資源使用

## 跨平台安全

- 驗證平台特定的安全要求
- 處理不同平台的權限模型
- 實現平台特定的安全檢查

## 程式碼安全

- 避免使用 `eval()` 或類似危險函數
- 使用 TypeScript 的嚴格模式
- 實現適當的錯誤處理

## 更新安全

- 實現安全的更新機制
- 驗證更新檔案的完整性
- 處理更新失敗的情況

## 隱私保護

- 不收集用戶個人資料
- 實現資料最小化原則
- 提供透明的隱私政策

## 安全測試

- 進行安全漏洞掃描
- 測試權限濫用情況
- 驗證輸入驗證機制